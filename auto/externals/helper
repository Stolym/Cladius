CXCOLOR0Commands:
    developer   - actual mode
    update      - (TO-DO) auto-update the version and push it on dev branch "update [0 = major, 1 = minor, 2 = patch] [commit msg]"
    convert     - (TO-DO) convert data to json
    load        - (TO-DO) allow us to load a model and test it "load -path_eval [path]"
    help        - list of available commands
    exit        - exit the programCXRESET